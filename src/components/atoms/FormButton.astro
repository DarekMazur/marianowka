---
interface Props {
	buttonMessage: string,
	id?: string
}

const {buttonMessage, id} = Astro.props
---

<button type="submit" id={id} class="w-full bg-farmstay-green text-white font-medium py-3 rounded-lg hover:bg-opacity-90 transition-all cursor-pointer">
	{buttonMessage}
</button>